{
  "name": "dsga1003",
  "private": true,
  "scripts": {
    "stylus": "stylus styles/",
    "watch": "stylus --watch styles/",
    "build": "bash ./build.sh"
  },
  "devDependencies": {
    "stylus": "0.49.3"
  }
}
